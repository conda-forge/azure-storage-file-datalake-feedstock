{% set name = "azure-storage-file-datalake" %}
{% set version = "12.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.zip
  sha256: f42e89219e17693f3ddfb6fb97c674e92a4a2244eac27c8fc5f4f92e967e686b

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools
  run:
    - python >=3.6
    - azure-core >=1.10.0,<2.0.0
    - msrest >=0.6.18
    - azure-storage-blob >=12.7.0,<13.0.0

test:
  imports:
    - azure
    - azure.storage
    - azure.storage.filedatalake
    - azure.storage.filedatalake.aio

about:
  home: https://github.com/Azure/azure-sdk-for-python/tree/master/sdk/storage/azure-storage-file-datalake
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: This preview package for Python includes ADLS Gen2 specific API support made available in Storage SDK
  doc_url: https://aka.ms/azsdk-python-storage-filedatalake-ref

extra:
  recipe-maintainers:
    - dhirschfeld
